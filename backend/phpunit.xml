<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap-testing-env.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
            <directory>Modules/*/Tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
            <directory>Modules/*/Tests/Feature</directory>
        </testsuite>
        <testsuite name="Architecture">
            <directory>tests/Architecture</directory>
            <directory>Modules/*/Tests/Architecture</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- APP_ENV/DB_* are set in tests/bootstrap-testing-env.php, not here:
             the quran_platform_app Docker container injects real
             DB_CONNECTION=mysql/DB_DATABASE=quran_platform values into
             $_SERVER, which Laravel's env() helper reads *before* anything
             declared in this <php> block (even with force="true", which only
             reaches getenv()/$_ENV) — so a plain <env> override here still
             silently lets the whole suite (RefreshDatabase and all) run
             against the real dev database. See SeasonActivationConcurrencyTest
             for the one deliberate MySQL exception, which opens its own
             explicit connection to a separate quran_platform_test database
             instead of relying on this default connection. -->
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SCOUT_DRIVER" value="collection"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
